# Day 4 lab data

`docs/*.md` — copied verbatim from `labs/day2/python/data/docs/` (Day 2's
Contoso developer API documentation, used there as the Foundry IQ
knowledge base source). Copied, not referenced, so Day 4's Retriever role
(`search_docs` in `roles.py`) has zero dependency on Day 2's lab state,
your own Azure resources, or any live knowledge base — just this repo
checkout. If Day 2's docs change materially, re-copy from there; this is
static reference content, not expected to need syncing often.
