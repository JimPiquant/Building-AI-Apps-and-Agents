[project]
name = "day1-lab"
version = "0.1.0"
description = "Day 1 lab — two agents, same job (client-side, PromptAgent, HostedAgent)."
requires-python = ">=3.11"
dependencies = [
    "agent-framework",
    "agent-framework-foundry",
    "azure-identity",
    "python-dotenv",
]

# Cohort 1 (Aug–Oct 2026) pinning happens via uv.lock (committed).
# See manifests/versions.md for the version policy.

[tool.uv]
package = false
